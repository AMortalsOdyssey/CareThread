# CareThread 实施日志

> 用途：按里程碑记录实现选择、验证证据与查漏结果，便于阶段 review。每个里程碑提交前更新；`PROGRESS.md` 记录结论，本文件保留过程与依据。

## 2026-07-30 / M0 骨架与基建

### 实现

- 用 XcodeGen 2.46.0 建立 iOS 17.0+、Swift 5 语言模式工程；生成的 `.xcodeproj` 不入库。
- 标准版先建立 5 槽根导航，大字版建立 3 槽根导航；显示模式可由 `@AppStorage` 持久化并可用 `-displayMode` 启动参数覆盖。
- 将设计规范 §3–§5 的基础色、语义色、类型色、字号、间距、圆角和尺寸落为 `CT` token；颜色全部进入带 Light/Dark 外观的 Assets catalog。
- 建立 `verify.sh`、`acceptance.sh`、`screenshots.sh` 与进度/阻塞文档。

### 验证证据

- `Scripts/verify.sh`：退出码 0。
- Swift Testing：1/1 通过；XCUITest：1/1 通过。
- Computer Use：在 iPhone 16 / iOS 18.6 模拟器打开 CareThread，核对五槽 Tab、暖纸白背景和医用青主色实际渲染。
- Git：`75ea563 M0: 骨架与基建完成`。

### 查漏

- AppIcon 仍是空槽，按任务书留到 M8 读取视觉资产规范后补齐。
- `screenshots.sh` 目前只有安全骨架，M8 接入 18 个确定性页面状态与双外观采集。
- `acceptance.sh` 的测试计数、截图计数、Info.plist、依赖白名单校验将在对应产物出现后逐步收紧，M9 必须一次性全 PASS。

## 2026-07-31 / M1 数据层

### 实现

- SwiftData 七类实体、值类型与关系。
- 固定时钟注入、年龄计算、附件 Vault 不可变写入、CRUD/删除文件调度、用药剂量调整链。
- 六记录故事线种子与 300 条确定性压测数据。

### 验证证据

- `Scripts/verify.sh`：退出码 0。
- Swift Testing：31/31 通过；XCUITest：1/1 通过。
- 记录三层数据、级联关系、删除文件调度与剂量调整链均经过内存 ModelContainer 真实往返。
- Vault 在模拟器沙盒真实写入工作副本和 `originals/` 原件，覆盖不可变、防目录穿越、缺失文件与孤儿扫描。
- 红线扫描：联网实现、未完成标识均零命中。

### 查漏

- [x] 内存 ModelContainer CRUD 与 cascade 行为
- [x] 2/29、生日缺失、生日晚于事件、手填年龄优先
- [x] Vault 路径防穿越、写入后不可覆盖、缺失文件与孤儿扫描
- [x] 剂量调整旧止期、新版本 `previousVersionId`
- [x] `verify.sh` 全绿且单测不少于 14（实际 31）

### 已知诊断

- iOS 18.6 模拟器在首次构造 SwiftData schema 时会输出数组属性的 Core Data materialization 诊断；实际插入、保存、重新抓取的数组字段均通过测试。当前不影响功能，后续在持久化库迁移与备份往返阶段再次复核。

## 2026-07-31 / M2 OCR 与提取引擎

### 实现

- 定型 `OCREngine`：统一输出按页面排序的 `TextBlock(text, box, confidence)`；Vision 使用 `.accurate`、`zh-Hans + en-US`、语言纠错，全程设备本地。
- `ExtractionEngine` 保持纯函数：输入 OCR 文本和固定“今天”，输出机器层快照；没有 UI、数据库或外部状态依赖。
- 六份虚构病程样张及期望 JSON 原样落入测试目录，覆盖病理、出院、检验、超声、门诊与 CT。
- 规则细分为日期上下文优先、医院最长匹配、科室标签优先、类型加权/并列回退、指标显式与推导异常分层、医嘱段高置信、相对复查日期折算。

### 红绿循环

- 首轮 85 个单测中 10 个断言暴露三类真实问题：检验缩写数字被误认成结果、单位 `/L` 被误认成低标记、超声强证据不足。
- 修复解析规则后保留全部断言重跑；没有删除、跳过或弱化测试。
- 最终 `Scripts/verify.sh`：Swift Testing 85/85、XCUITest 1/1，退出码 0。

### 查漏

- [x] 六类样张类型、日期、医院、科室与关键字段
- [x] 指标 ↑/↓/H/L、区间推导与电话号码排除
- [x] 历史用药低置信、医嘱/处理段高置信
- [x] 1 月/3 月/2 周复查折算与“定期复查”不虚构日期
- [x] 空文本、未来日期、无效日期、10k 字性能
- [x] 至少 2 个真实 Vision OCR 闭环与空白图

### 下一阶段入口

- M2 协议现已满足 OCR 独立任务前置条件。按 07 的硬门槛和评分公式启动候选许可证/维护性核验与虚构测试集基准；若挑战者不同时越过换引擎门槛，唯一结论自动维持 Vision。

## 2026-07-31 / 范围增补与审查启动

### 新增范围

- 用户要求当前交付增加最多 20 位成员的本地独立管理与快速切换。
- OCR 入库新增姓名一致性闸门：不一致时必须切换；仅“姓名识别错误”二次确认允许留在当前成员，并记录识别姓名、当前成员、原因与确认时间。
- `＋` 的更多功能新增复查提醒、用药提醒、本地通知与可选日历事件。
- 每位成员的原件 Vault 作为真实数据源，机器提取和人工确认均可重建；原件支持高清查看、系统导出/复制和 Files 目录访问。
- PDF 可按近 1 月、半年、1/2/5/10 年、全部导出单个成员资料，并使用系统分享面板交给微信等目标。
- 增加同一成员的本地跨期指标/事实对比；界面明确说明是离线规则整理、不是诊断或云端高级分析。
- 图片、拍照和文档继续支持；视频明确拒绝。

### 设计约束

- 当前成员只是查询/录入上下文，所有持久化对象必须显式携带不可空 `patientId`，不能依赖全局筛选器假定隔离。
- 原件目录以不可猜 UUID 作为磁盘键，不用姓名建路径；导出临时文件不得成为第二真实数据源。
- OCR 错配覆盖是受控例外，不是“强制添加”：需要用户主动选择原因并二次确认，且记录不能静默抹除审计信息。
- 提醒模型与系统通知/日历标识分离，权限拒绝时资料仍可保存；重复调度必须幂等。
- 所有导出、比较、搜索、删除、备份均先以 `patientId` 限定范围，再应用时间条件。
- 列表查询增加日期、病种/类型、医院、医生、事件年龄组合条件及确定性排序；分页游标绑定成员、筛选摘要和排序摘要，任一变化即作废。

### 并行审查

- 已启动数据安全/架构、UX/大字版、边界与对抗测试三路只读审查；根任务统一吸收结论后再实现，避免并行编辑覆盖。
- 用户允许项目全程轮换使用最多 20 个 subagent；受运行环境并发上限影响，同一时刻保持根任务加 3 个专项，完成后轮换性能/崩溃等审查。
- 新范围追踪见 [`SCOPE_ADDENDUM.md`](SCOPE_ADDENDUM.md)。

## 2026-07-31 / OCR 独立选型

### 基准实现

- 固化 6 张 P、16 张 H、8 张 D 和 1 张观察发票，全部来自虚构文本；系统三种指定字体均按原协议使用。
- Vision 和 RapidOCR PP-OCRv5 mobile 各预热后逐页跑 3 次；Rapid 模型逐一校验 SHA-256，推理期封死 socket。
- OCR 输出统一进入仓库实际 `ExtractionEngine`，统计日期、医院、类型、关键指标四类字段，没有为了候选修改提取规则。
- 新增独立 `OCRBench` iOS scheme；Vision 在 iPhone 16 / iOS 18.6 模拟器完成 30 页 × 3 次，P95 695 ms，指标保存在 xcresult 常驻附件并导出为 JSON。

### 结果与固定规则

- Vision：打印 CER 5.98%、手写 CER 3.92%、字段 113/120、Mac P95 229 ms。
- RapidOCR：打印 CER 2.34%、手写 CER 2.37%、字段 105/120、Mac P95 640 ms、arm64 增量上界 48.9 MB。
- 加权分：RapidOCR 79.74，Vision 40.00；Rapid 是分数胜者。
- 换引擎门槛：手写只改善 1.56 个百分点，低于必需的 10 个百分点；打印改善 3.64 个百分点。两条未同时通过，按 §5 唯一决定维持 Vision。
- ML Kit 因官方条款允许联系 Google 服务器并发送性能/使用指标而触发零联网红线；Tesseract iOS 包装因归档/旧上游出局；chineseocr_lite 因 GPL-2.0 出局。

### 交付证据

- 原始逐页输出、评分 JSON 与 Markdown 均在 `Benchmarks/OCRBench/results/`。
- 仓库报告 `docs/调研报告_OCR选型.md` 与交付包指定报告 SHA-256 相同：
  `db560fdce34cf212ee14c9b8b38c2975977eee42d34e5b102f27806f414f6e06`。
- 发布实现无第三方 OCR 依赖、无模型包、无运行时下载和遥测；`OCREngine` 协议保留未来替换位。
- 配置加入独立基准 target 后重跑主回归：`Scripts/verify.sh` 退出码 0，
  Swift Testing 85/85、XCUITest 1/1；`Scripts/acceptance.sh` 当前阶段五项均 PASS。

## 2026-07-31 / 主架构合同与需求追踪

### 用户追加

- 一期坚持 Swift/SwiftUI/SwiftData 等纯原生 iOS；Android 只作为二期，领域格式为未来迁移保持稳定，不引入跨端框架。
- GitHub 使用 `AMortalsOdyssey`，仓库必须公开，并按里程碑 commit/push。
- README 保持产品定位、功能、构建、里程碑和文档导航的稳定结构；实现过程与证据进入专门文档。
- 新增 iPhone 对 iPhone 的附近同步/换机，支持单成员或全部成员。

### 主 Agent 决策

- 建立 `ARCHITECTURE_CONTRACT.md`，统一多成员、姓名闸门、Vault、分页、资源、提醒、导出与性能门槛。
- 建立 `REQUIREMENTS_TRACEABILITY.md`，把全部用户引导拆成 A–N 编号并绑定实现、测试和证据。
- 附近同步主线从初步考虑的 MultipeerConnectivity 改为 Apple 当前推荐的 Network.framework；原因是 Apple 已把旧框架核心 API 标记 Deprecated。
- 零联网红线修订为零互联网/零服务器；Bonjour/点对点传输只在用户主动进入同步页面后短时开启。

### GitHub 证据

- 恢复 AMO GitHub CLI OAuth 后创建 `AMortalsOdyssey/CareThread`。
- `gh repo view` 复核：`visibility=PUBLIC`，`defaultBranch=main`。
- 首次 push 保留 M0、M1、M2 与 OCR 四个独立历史提交；当前未提交的数据模型与文档未混入。

## 2026-07-31 / 多报告、多页与多截图增补

- 用户指出一份报告可能需要多次截图，一次导入又可能同时包含多份、每份多页，而且只有首页有个人信息。
- 主 Agent 决定采用“操作分组权威、OCR 建议辅助”：ImportBatch → 多个 DocumentDraft → 有序 CapturePage。
- 照片/PDF 多选后必须进入整理页；支持拆分、合并、排序、去重复、旋转和原图查看，不把拖拽设为唯一交互。
- 姓名证据只在同一已确认文档组内继承。首屏 match + 续页 noEvidence 可继续；任一 mismatch 或多姓名 ambiguous 整组拦截。
- 自动建议使用页码、报告号、姓名、医院/日期/标题和截图文字重叠；不确定时明确交给用户确认，绝不静默分组。
- 大字版使用逐页大按钮；默认单批 100 页、单文档 50 页，并纳入 48MP/长 PDF 性能测试。
- 方案和自动化矩阵见 `MULTIPAGE_IMPORT_DESIGN.md`，追踪项见 F03a–F03f 与 L07。

## 2026-07-31 / iCloud、全字段编辑与社区需求增补

### iCloud 结论

- 核查 Apple 当前 App Review Guidelines 5.1.3(ii)，原文明确禁止 App 在 iCloud 存储个人健康信息。
- 本期唯一决定是不接 CloudKit、iCloud Documents、iCloud KVS 或相应 entitlement；真实 Vault/数据库/缓存执行备份排除复核。
- 系统日历账户同步和用户主动把导出副本分享到某处是独立系统/用户行为，UI 必须提示，不能包装成 CareThread iCloud 同步。
- 可验证的换机主线维持附近加密迁移与用户主动加密备份。

### 手动编辑合同

- 所有用户业务内容均提供编辑入口；稳定 UUID、成员归属、不可变原件、哈希、原始 OCR 快照和归属审计等证据字段不开放普通编辑。
- 编辑服务采用 expected revision，实体更新与修订历史同事务提交；撤销追加新修订，不抹除历史。
- 机器提取和人工确认分层，重新 OCR 不能覆盖用户更正。

### 社区调研与范围决定

- 检索国内少数派、知乎/V2EX和海外 Reddit、Product Hunt。重复痛点集中在：纠错成本、多成员资料、跨年时间线、就诊前摘要、用药/过敏清单、问题笔记、提醒以及无账号时的数据恢复。
- 纳入就诊准备卡、问题清单/就诊笔记、记录关联提醒、补药提醒、多病种、指标别名人工确认、待整理收件箱、重复提示、本地搜索、重要置顶和备份提示。
- 拒绝云端实时协作、AI 医学解读、医院门户同步、诊室录音、机构计费与法律资料管理，避免违反离线非诊断边界。
- 完整来源和采纳矩阵见 `USER_NEEDS_RESEARCH.md`，追踪项见 O01–O08、P01–P09 与 J13–J14。

### 上架资质报告落盘

- 复核 06 任务书要求后确认报告完整覆盖五个问题、A/B/C 三条路线、产品反向要求和时间行动清单。
- 将仓库报告复制到任务书指定的 iCloud 交付目录；`cmp` 返回 0。
- 两份文件 SHA-256 均为 `aeec732ae4eb329c697ff9e13eb6a2f07032356baab135a77da025e3fa49b835`。

## 2026-07-31 / 开源、商业化与出售路线落地

- 用户要求在主开发完成后评估非盈利 App 的开源价值、社区信任、Android 扩展、套壳/版权风险与未来商业化。
- 总清单新增 Q01–Q11，要求覆盖四种源码路线、iOS/App Store 许可兼容、商标/软件著作权、同类产品、买断/一次性内购/订阅、未来 AI 三种部署模式，以及开源推广后出售项目的退出路线。
- 已输出 `OPEN_SOURCE_AND_SUSTAINABILITY_STRATEGY.md`，形成唯一发展路线：MIT 本地核心先免费开源建立信任，达到 12 个月指标后官方 App Store 版采用一次性买断，24 个月达到权利/安全/经营门槛后建立数据室，36 个月达到成交门槛后出售产品资产；未达标进入低成本维护。
- 当前仓库继续使用 MIT License；报告明确 MIT 允许商业复用，不能追加“禁止商用/套壳”限制，历史版本按既有授权不可收回处理。防混淆依赖品牌、商标、官方发行身份、稳定质量和可核验隐私记录。
- 已加入 `CONTRIBUTING.md` 与 `TRADEMARKS.md`。ICLA/CCLA 未就绪前只收 issue/虚构复现；未来实质贡献必须同时经过 DCO 和 CLA，确保持续开源、再许可与出售的 IP 权利链。
- 出售资产固定为代码/设计权利、品牌/商标/域名、GitHub 社区、App Store App/Bundle ID/评价、聚合经营指标和质量体系；用户设备健康资料、个人 Apple 开发者账号、证书和凭据永不进入交易包。
- 报告逐项比较 MIT、Apache-2.0、MPL-2.0、GPL/AGPL、双许可和 source-available，也比较免费、买断、IAP、版本收费、订阅及未来 AI 扩展；本地核心唯一收费方式为门槛后的 App Store 一次性买断。

## 2026-07-31 / M3–M9 分阶段交付

### 提交与能力

- `40450bd M3`：离线录入、多报告/多页整理、姓名闸门、资料库、组合筛选、稳定分页、草稿与不可变原件。
- `70c534a M4`：用药、医嘱、剂量修订、关联复查与本地提醒。
- `b95e493 M5`：复查计划、权限恢复、系统日历入口和首页。
- `61bddaf M6`：成员隔离的时间线、筛选、月份定位和 Reduce Motion。
- `cba6a36 M7`：七档导出、本地对比、系统分享、一页就诊准备卡与问题清单。
- `5709ce2 M8`：加密备份/恢复、完整性校验、应用锁与标准版打磨。
- `f6c3361 M9`：首次引导、完整大字版、无障碍、边界门禁和总验收。
- `0274e64`：补齐原件缺失的备份恢复入口；把私有 `.SFUI-*` 字体改为公开 `UIFontMetrics` 动态字体管线。
- `9a71871`：增加标准版 AX3 首页/核对/详情三段真实 XCUITest。

所有提交均由 `AMortalsOdyssey` 创作、无 `Co-Authored-By`，并在 `main` 分阶段推送到 Public 仓库。

### 专项审查吸收

- 安全/隐私：跨成员闭包、路径穿越/链接/压缩炸弹、篡改、重放、CAS、日志隐私、Data Protection 和备份/迁移事务均补充对抗测试。
- 性能/崩溃：300×2 记录/100 次切换、100/50/22 页、48MP、250MiB PDF、16MiB Nearby、8MiB 备份、300 条 PDF 和 100 轮 staging 清理成为可重复代理。
- 就诊准备卡：八类可选内容、固定内容预算、严格单成员隔离与一页 PDF。
- 设计：颜色/间距 token、Light/Dark、一步原文、关键文案、AX3/AX2、Reduce Motion、大字版无隐藏手势均固化为测试与走查证据。

## 2026-07-31 / 首次模拟器验收（整改前基线）

### 全量回归

- `Scripts/verify.sh`：退出码 0。
- Swift Testing：608/608；XCUITest：39/39；零失败、零跳过。
- 构建目标：iPhone 16 / iOS 18.6；结果包：本地 `DerivedData/Verify.xcresult`。
- 边界矩阵：23/23 项均由 `Scripts/acceptance.sh` 绑定到真实测试方法。

### 截图与人工走查

- `Scripts/screenshots.sh`：36 张真实模拟器 PNG；标准版 28、大字版 8；Light/Dark 各 18；尺寸 1179×2556；36 个不同 SHA-256。
- `docs/SCREENSHOT_MANIFEST.json` 绑定源码提交 `9a71871`、源码指纹、设备/runtime、路由、就绪标记与每图哈希，`sourceTreeDirty=false`。
- 视觉检查 36 屏：没有空白、错误路由、字体回退、大字版裁剪或主操作缺失。
- `docs/MANUAL_WALKTHROUGH_EVIDENCE.json` 将设计 §14 八项与 B9/B16/B17/B18/B23 的 13 份证据逐项哈希固定。
- Reduce Motion 在模拟器中临时开启后检查时间线路由，完成后恢复默认。

### 权限与降级记录

- 按用户要求调用 Computer Use；Mac 处于锁定状态，工具明确拒绝自动解锁。
- 未绕过锁屏或伪造点击；使用 simctl、XCUITest、xcresult 和静态/视觉交叉证据继续闭环。
- 需要相机、FaceID、通知真实到达、EventKit、微信、无线链路、低存储/锁屏与长辈真人的项目保留在 `PROGRESS.md` 末尾。

### 最终查漏

- [x] M0–M9 独立提交并推送
- [x] 608 单元/集成测试与 39 UI 测试全绿
- [x] 23 项边界矩阵自动化
- [x] 36 张截图与可复核 manifest
- [x] 13 项设计/边界走查证据
- [x] README、进度、追踪表、阻塞与商业化文档同步
- [x] MIT License、品牌边界和未来出售路线落仓库
- [ ] 真机/真人清单由用户在具备设备和系统权限时执行

## 2026-07-31 / Review 整改、重复拦截与最终收口

### P0/P1 整改结果

- P0-1 至 P0-6 全部落地：环境要求与运行时诊断、记录行信息、详情/种子、全中文、FAIL → FIXED 走查和 UI 测试库隔离均有测试或截图证据。
- P1-6 至 P1-14 全部落地：Vault 单一权威、领域文件名、46 张截图、OCR 真实集 v2、D1–D4、＋面板、生产导航壳、三档外观和键盘焦点重试均已闭环。
- `RecordOrderFullEditUITests` 两条用例连续三轮全绿；全量回归再跑一轮仍通过，没有通过删除、跳过或放宽断言换绿。

### 上传资料三层查重

- 第一层是原件 SHA-256：同一批次或同成员历史精确命中属于不可覆盖的硬重复；提交事务再次检查。
- 第二层是完全离线、带算法版本的视觉内容指纹：DCT/block/dHash 配合多裁切与旋转归一，覆盖 PNG/JPEG 重编码、重新拍照、裁边和宽高比变化；长 OCR 失败不会关闭视觉兜底。
- 第三层是规范化 OCR 内容重叠：比较三元组、页面包含和当前多页聚合；日期或报告号明确冲突时抑制同医院模板误报。
- 视觉/OCR 命中只给疑似重复提示，用户明确确认“不是重复”后才可继续，且确认前重扫；精确重复没有强制添加入口。检查取消、暂存引用损坏、历史原件不可读或语料持续变化全部失败关闭。
- 查重只读取当前成员。大字版无法安全处置时保留草稿、journal 和 staging 原件，引导切到标准版复核。纯手工录入只有在无图片、无 OCR 和无机器证据时才可旁路。
- `CaptureDuplicateDetectorTests` 20 条专项测试使用虚构图像/文本，覆盖精确哈希、重拍、旋转、裁边、重编码、多页单页重传、聚合文本、全半角、模板误报、跨成员隔离与损坏失败关闭。

### PDF 品牌钩子与备份边界

- 多页病程 PDF 与一页就诊准备卡复用 `CareThreadPDFBranding`：页首横线、Logo、产品名；末段 Logo、产品名、介绍与二维码。
- 官网尚未上线，二维码暂指向公开项目页；URL 集中为单一常量。测试从最终 PDF 页面栅格化后解码二维码，确认打印/分享链可扫描，不只测试原始二维码图片。
- Vault 原件的 BSD immutable 标志不再被复制到备份临时文件；导出改为有界分块复制字节到新受保护文件，异常/取消清理目标。28 条备份单测和 3 条备份/锁 UI 聚焦复验共 31/31 通过。

### 最终证据

- `Scripts/verify.sh`：Xcode 26.6，iPhone 16 / iOS 26.5，651/651 单元与集成测试、50/50 XCUITest，合计 701/701；失败 0、跳过 0。
- `Scripts/screenshots.sh`：23 个生产导航路由 × Light/Dark = 46 张；标准版 38、大字版 8，全部 1179×2556 且 SHA-256 唯一，清单绑定源码提交 `57b28a6`。
- `Scripts/validate-screenshot-manifest.sh` 与 `Scripts/validate-walkthrough.sh` 均 PASS；B1–B7 证据保留修复前失败事实，不以最终绿覆盖历史。
- `Scripts/acceptance.sh` 在提交 `1c14406` 的干净工作树原样运行并退出 0；M0–M9、701/701 测试、23/23 边界、依赖白名单、零互联网、日志/路径/秘密、权限文案、Privacy Manifest、46 张截图与走查证据全部 PASS。
- 仍需真实设备的相机、FaceID、通知/日历、微信、双机无线、锁屏文件保护、真实病历 OCR 与长辈真人试用保持在 `PROGRESS.md` 末尾，不以模拟器代理冒充。

### 总验收脚本自检

- 证据提交 `d95184b` 后首次从干净工作树运行 `Scripts/acceptance.sh`：M0–M9、干净工作树、`verify.sh`、651/651 单元与集成测试、50/50 UI 测试、零联网与隐私门禁均先通过。
- 后半段发现两个脚本问题：`.build/DerivedData` 是 Git 已忽略的本机构建缓存，却被 vendored 二进制扫描纳入；截图 PASS 文案中的未加花括号变量紧邻中文全角括号，被 Bash 解析成未绑定变量。
- 修复提交 `57b28a6`：所有相关扫描显式剪枝 `.build`，中文标点相邻变量统一加 `${...}` 边界；772MB 缓存移至 `/tmp`。`bash -n`、46/38/8 变量展开与独立 vendored 扫描均通过。
- 因脚本与源码都属于截图指纹输入，未复用旧 manifest；重新生成 46 张截图并把清单、走查证据绑定到 `57b28a6`。最终在提交 `1c14406` 上执行原命令，退出码 0，末行是 `PASS CareThread 最终验收（全部检查通过）`。

## 2026-07-31 / 第二轮落地批次 1

- 先完整复核 10–13 号任务书并锁定最终决策，确认 `main = origin/main = 354bba6`、工作树干净后开工。
- 真实 OCR 样本目录只做元数据门禁：40 张、36,103,220 bytes、HEIC/JPEG/PNG，未输出文件名或 OCR 原文，也未复制到仓库。
- 模型拆分采用原代码块机械迁移，5 个聚合文件合计仍为 15 个 `@Model`；构建成功证明 SwiftData 嵌套类型与跨文件关系可解析。
- PDF 文案与品牌 token、模式术语清理、`CTCapability` 能力门禁同步补齐回归测试。
- 批次一冻结前在 iPhone 16 / iOS 26.5 上完整执行 `Scripts/verify.sh`，退出码 0；653/653 单元与集成测试、50/50 XCUITest，合计 703/703，失败 0、跳过 0。
- 验证命令：generic iOS Simulator build；`CTCapabilityTests`、`M7PDFExportTests`、`VisitPreparationCardTests` 共 13 条，全部通过。

## 2026-07-31 / 第二轮落地批次 2

- 备份信息架构先放“换新手机”，再放默认无口令存档；敏感性警告与可选加密紧邻对应动作，避免把应用锁、导出文件口令和迁移配对混为一谈。
- 恢复文件由安全作用域复制到 App 私有临时目录后再识别格式与校验，副本启用完整文件保护并排除系统备份；完成、失败、取消和页面退出均清理副本。
- 可选口令最初使用系统折叠组件，但 UI 自动化无法稳定发现折叠内容；改为语义等价的显式展开按钮并保留无障碍值后，完整备份/应用锁 UI 套件 6/6 通过。
- 换机后的应用锁建议使用独立本机一次性状态，不进入备份、Nearby manifest 或密文载荷。接收完成事件只负责触发条件判断，确认动作继续调用既有应用锁启用实现。
- 生物识别“是否可提示”与应用锁“是否可启用”分离：前者只接受生物识别，后者继续接受 Face ID/Touch ID 或设备密码，覆盖仅设备密码的可用性边界。
- 聚焦验证：备份/应用锁单测 43/43，备份/应用锁 UI 6/6，大字版设置 UI 1/1，Nearby 一次性提示 UI 1/1；`git diff --check` 通过。
