# CareThread 阻塞记录

当前无代码实现阻塞；存在三项 **App Store 正式提交前** 的法律事实阻断：

- 锁定隐私政策/用户协议写的是备份默认使用至少 12 位口令加密，当前产品按 2026-07-31 最终决定采用默认可读存档、口令加密可选。
- 当前相册导入使用系统 `PhotosPicker`，只读取用户明确选择的项目，没有请求整个照片库授权；锁定政策与权限用途仍写成“从相册导入时申请照片权限”。
- Cloudflare 官网的两份协议页使用当前默认可读存档口径和 `founder@8xd.io`，锁定 Markdown/App 本地资源仍使用默认口令加密口径和 `jianghaibo@multiego.me`；当前任务同时禁止改写两边正文，无法在本轮消除这项语义差异。

本轮按任务书要求以代码和最终产品决定为准，不擅改锁定正文、官网正文，也不回退已经验收的产品行为。正式上架前必须由产品/法务形成新版本正文，同步更新最后更新日期、`LegalAgreement.currentTermsVersion`、App 本地资源与 Cloudflare 官网，再完整复跑法律版本变更、协议锁定和权限测试。详细操作见 [`APP_STORE_SUBMISSION.md`](APP_STORE_SUBMISSION.md)。

`Scripts/acceptance.sh` 在提交 `45136ea` 的干净工作树上完整复跑后精确为 1 项失败：同一个法律语义同源门禁覆盖上述三项差异；其余测试、边界、依赖、零联网、隐私、截图和走查门禁全部通过。这是有意的发布失败关闭，不是测试基础设施故障；在两份锁定正文与官网正文的“不得改写”红线解除前，不得通过删除检查、改成警告或只比较日期/标题来换绿。

设备模拟器验收脚本已以 `FAIL=0 / RESIDUAL=4` 收口：107/107 聚焦测试、通知 4/4 到达、EventKit 写入/删除、系统分享、备份 ZIP、5 页 PDF、应用锁与双模拟器入口已经覆盖。四项残留分别是完整照片库授权的模拟器 TCC 差异、3/4 通知点击未把 App 置前、冷启动通知点击未唤起 App、48MP 图片未从系统 `PhotosPicker` 返回添加状态/RSS；双机 AWDL、微信、锁屏视觉、Jetsam/热压力和大字版真人试用继续作为真机证据边界。完整清单见 [`PROGRESS.md`](PROGRESS.md)。
